language: php
php:
  - 5.5
  - 5.6
  - hhvm

before_install:
  - composer install
  - bundle install

before_script:
  - php artisan serve &
  - bundle exec chef-zero -d

services:
  - redis-server

script:
  - ./vendor/bin/behat -p travisci
