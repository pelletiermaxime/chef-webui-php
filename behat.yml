default:
    formatters:
        pretty: true
        # parameters:
        #     decorated:              true
        #     verbose:                false
        #     time:                   true
        #     language:               fr
        #     output_path:            null
        #     multiline_arguments:    true
    autoload:
        - %paths.base%/app/tests/features/bootstrap
    suites:
        test:
            paths:
                - %paths.base%/app/tests/features
    extensions:
        Behat\MinkExtension:
            # selenium2: ~
            goutte: ~
            # zombie: ~
            # sahi: ~
            base_url: http://localhost:8000
            show_cmd: firefox-bin %s
    gherkin:
        filters:
            tags: "~@wip"
